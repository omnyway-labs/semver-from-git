#!/bin/bash

export SEMVER_FROM_GIT_HOME="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.."

$SEMVER_FROM_GIT_HOME/node_modules/lumo-cljs/bin/lumo -K  -c src:"$SEMVER_FROM_GIT_HOME"/lib/tools.cli-0.3.5.jar:"$SEMVER_FROM_GIT_HOME"/lib/fs-0.1.1.jar -m semver-from-git.core $@
